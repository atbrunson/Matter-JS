/**
 * @module Calculation
 * @description try's to process the variable and regular expression before each Matter.js update 
 * @example this.update = new Calculation{
 * 
 * } 
 */


class Calculation{
	constructor( {variables}, regex) {

		
		for (v in variables){
			temp = v => regex
		}




		// add event watcher and execute "beforeUpdate"
		// add method to perform calculation
	}


}